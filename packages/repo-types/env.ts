/**
 * For ease of use since this repo will never leave
 * the local dev environment I will use a ts file for
 * env variables instead of a .env file
 */

export const FRONTEND_PORT = 3000;
export const BACKEND_PORT  = 8080;