import { AppendEmojisPipe } from './append-emojis.pipe';

describe('AppendEmojisPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendEmojisPipe();
    expect(pipe).toBeTruthy();
  });
});
